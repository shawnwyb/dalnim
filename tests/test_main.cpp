// Exists only to generate main() for the test binary. Exactly one file may do this,
// so the tests themselves live in their own files beside it.
#define DOCTEST_CONFIG_IMPLEMENT_WITH_MAIN
#include <doctest/doctest.h>
